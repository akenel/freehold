# Freehold login theme — inherits Keycloak's stock look, adds languages Keycloak
# doesn't ship (e.g. Hindi). This is the "extend a plugged-in package" pattern:
# we don't fork Keycloak, we drop a message bundle beside it.
parent=keycloak
